
const initialState = {
    users: [],
    isLoading: false,
    error: '',
    posts: []
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        
        default: 
            return state;
    }
}


